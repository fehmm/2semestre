bibliocas: 

#include <stdio.h> // entrada e saida padrão do C 

#include <stdlib.h> //locação de memória: malloc(), calloc(), realloc(), free() 
//→ Usadas para criar e liberar memória na hora da execução.

//Conversão de strings para números:
//atoi(), atof(), atol()
//→ Transformam texto (como "123") em número (como 123).

//Funções de controle do programa:
//exit(), abort()
//→ Servem para encerrar o programa imediatamente.

//Geração de números aleatórios:
//rand(), srand()
//→ Para gerar números pseudoaleatórios.

//Ordenação e busca:
//qsort(), bsearch()→ Permitem ordenar ou procurar elementos em arrays.

#include <time.h> //datas e horas

--------------------------------------------------------------------------------------------=

if e else 

// if (variave = 6) {
    //printf //exemplo
// } else {
   // printf//exemplo
// }

SWITCH E CASE 

//   switch (expression)
//   {
//   case constant expression:
    /* code */
//   break;

//  default:
//   break;
//  }

while (condition)
{
    /* code */
}
