#include <stdio.h>
#include <math.h>
#include <stdlib.h>

// definição                    system("cls") limpa as caracteres do terminal

typedef struct
{
    int numerador;
    int denominador;

}fracao;


fracao criarfracao(int N, int D) {
void exibirfracao (fracao F);

int main(){

    system("cls";)

    fracao F1 = criarfracao(24,30);
        F1 =  simplificarfracao(F1);
        exibirfracao(F1); 

        fracao f2 = criarfracao (-30,40);
        F1 = simplificarfracao(F2);
        exibirfracao(F2);
    return 0;
}
}


fracao criarfracao(int N, int D) {
    
  F.numerador = N;
  F.denominador = D;
return F;

}



 
 fracao F1 = criarfracao(7,2);
 printf("o valor de F1 E %f.\n",(float) F1.numerador/F1.denominador);
    


